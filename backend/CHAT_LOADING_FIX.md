# 🔧 Исправление загрузки чатов

## Проблема

После авторизации чаты не загружались:
1. Store источник не находился (Store not found)
2. CDP Network возвращал 0 чатов
3. DOM fallback не срабатывал автоматически

## Исправления

### 1. StoreChatSource - Добавлено ожидание инициализации

**Проблема:** Store может быть не инициализирован сразу после загрузки страницы.

**Решение:**
- Добавлено несколько попыток (5 попыток) с экспоненциальной задержкой
- Улучшена детекция Store (проверка нескольких возможных путей: `window.Store`, `window.WWebJS`, `window.webpackChunkwhatsapp_web_client`)
- Добавлено детальное логирование для диагностики

**Файл:** `backend/app/services/whatsapp/parsing/sources/store_chat_source.py`

### 2. CDPNetworkChatSource - Улучшена загрузка чатов

**Проблема:** CDP Network не получал данные, потому что чаты не загружались автоматически.

**Решение:**
- Добавлен триггер загрузки чатов (скролл списка чатов)
- Увеличено время ожидания с 2 до 5 секунд
- Добавлена периодическая проверка наличия чатов

**Файл:** `backend/app/services/whatsapp/parsing/sources/cdp_network_chat_source.py`

### 3. Orchestrator - Добавлен автоматический fallback

**Проблема:** Если CDP Network возвращал 0 чатов, система не переключалась на DOM fallback.

**Решение:**
- Добавлена логика автоматического переключения на DOM источник, если CDP Network возвращает 0 чатов
- Улучшено логирование процесса fallback

**Файл:** `backend/app/services/whatsapp/parsing/orchestrator.py`

## Ожидаемое поведение

1. **Store источник (приоритет 1):**
   - Пытается найти Store с 5 попытками
   - Если найден - использует его (самый надежный источник)

2. **CDP Network источник (приоритет 2):**
   - Используется если Store недоступен
   - Триггерит загрузку чатов
   - Ждет до 5 секунд для получения данных
   - Если возвращает 0 чатов → автоматически переключается на DOM

3. **DOM источник (приоритет 3 / fallback):**
   - Используется если Store и CDP Network недоступны
   - Или если CDP Network вернул 0 чатов
   - Парсит чаты напрямую из DOM

## Тестирование

После этих изменений система должна:
1. ✅ Успешно находить Store после нескольких попыток
2. ✅ Получать чаты через CDP Network с триггером загрузки
3. ✅ Автоматически переключаться на DOM если другие источники не работают
4. ✅ Загружать чаты хотя бы через один из источников

## Логи для диагностики

При проблемах проверьте логи на наличие:
- `"Store source initialized: total_count=..."` - Store найден
- `"CDP Network source initialized, listening for network responses"` - CDP инициализирован
- `"DOM fallback successful: collected ... chats"` - DOM fallback сработал
- `"Fetched X chats from ..."` - количество загруженных чатов

