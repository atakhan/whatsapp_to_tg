# 📄 **Техническое задание: “WhatsApp → Telegram Migrator”**

* Backend: **Python + FastAPI**
* Frontend: **Vue.js**
* Перенос WhatsApp → Telegram
* Со всеми медиа, в максимальном качестве
* Простой UX для обычного пользователя

## 1. Цель проекта

Создать веб-приложение, которое позволяет обычному пользователю перенести чат из WhatsApp в Telegram, включая **текст**, **фото**, **видео**, **аудио**, **документы**, **голосовые**, **стикеры**, **GIF**.
Приложение должно обеспечивать гарантированную передачу всех медиа в максимальном качестве.


---

# 2. Архитектура

## 2.1. Общая схема

```
Browser (Vue.js)
     |
     | HTTPS
     |
Backend (FastAPI, Python)
     |
     | Telegram MTProto Client (user session)
     |
Telegram Servers
```

## 2.2. Основные компоненты

### Backend (Python. FastAPI)

Модули:

1. **Auth module** — Telegram OAuth Login Widget → получение Telegram user ID → инициализация MTProto client.
2. **Upload module** — загрузка ZIP экспорта WhatsApp.
3. **Parser module** — разбор ZIP:

   * чтение `*_chat.txt` или `messages.json`
   * корректный парсинг сообщений и медиа
   * восстановление хронологии
4. **Media handler** — сохранение медиаконтента во временную папку.
5. **Telegram sender**

   * отправка в выбранный чат/контакт
   * отправка в максимальном качестве (*sendDocument*)
   * поддержка группировки медиа в альбомы
6. **Migration manager**

   * очередь сообщений
   * троттлинг
   * прогресс миграции
7. **Cleanup**

   * удаление временных файлов после миграции

### Frontend (Vue.js)

Модули:

1. Welcome page
2. File upload page
3. Telegram login page
4. Chat selection page
5. Migration status page (progress bar + лог)
6. Result page (успех / ошибки)

---

# 3. Функциональные требования

## 3.1. Процесс пользователя (UI/UX Flow)

1. Пользователь открывает сайт.
2. Нажимает **“Начать перенос”**.
3. Загружает ZIP экспорт WhatsApp чата.
4. Авторизуется через Telegram Login Widget.
5. Приложение получает список контактов Telegram.
6. Пользователь выбирает чат для переноса:

   * существующий контакт
   * Saved Messages
   * новый приватный чат
7. Пользователь нажимает “Start Migration”.
8. В реальном времени отображается прогресс:

   * % обработанных сообщений
   * количество отправленных медиа
   * бар загрузки
9. По завершении:

   * кнопка “Открыть чат”
   * лог ошибок (если были)

---

# 4. Функциональность backend

## 4.1. Авторизация Telegram

Использовать:

### **Telegram Login Widget**

* выдаёт auth data (ID, имя, контрольная подпись)
* backend валидирует подпись по алгоритму Telegram
* создаёт MTProto сессии для пользователя:

  * MadelineProto (PHP) — нельзя
  * GramJS — нельзя (JS)
  * **Pyrogram / Telethon — ДА**

Выбираем: **Telethon** (стабильнее)

Создаётся файл `sessions/<user_id>.session`.

---

## 4.2. Приём экспорта WhatsApp

Backend принимает:

* ZIP до **20GB**
* Внутри:

  * `*_chat.txt` или `messages.json`
  * папки `Media`:

    * Images
    * Videos
    * Audio
    * Documents
    * Voice Notes
    * Animated GIFs
    * Stickers

Файлы сохраняются во временную директорию `/tmp/<session_id>/`.

---

## 4.3. Парсер WhatsApp экспорта

### Поддерживает:

* форматы txt
* форматы JSON (новый формат iOS)
* таймстемпы в разных локалях
* многострочные сообщения
* вложения с потерянными ссылками (восстанавливаем по имени файла)
* альбомы (несколько фото подряд от одного автора в одну минуту)

### Структура сообщения после парсинга:

```json
{
  "timestamp": 1623452345,
  "sender": "John Doe",
  "type": "text|image|video|audio|voice|document|gif|sticker",
  "text": "...",
  "media_path": "/tmp/.../Media/Images/IMG1234.jpg",
  "is_album": true|false,
  "album_group_id": "abc123" 
}
```

---

## 4.4. Отправка в Telegram (MTProto)

### Общие требования:

* Отправка **в личные чаты и Saved Messages**
* Текст → `sendMessage`
* Фото → `sendDocument` (НЕ `sendPhoto`)
* Видео → `sendDocument`
* Аудио → `sendDocument`
* Голосовые → `sendVoice` (если пользователь хочет “реконструкцию”)
* Стикеры → `sendDocument`
* GIF → `sendDocument` с `mime_type="video/mp4"`

### Поддержка альбомов

Если WhatsApp отправлял несколько фото подряд (< 2 минуты):

* объединить в Telegram album (MediaGroup)
* использовать `sendMediaGroup`

### Ограничение Telegram (1 секунда между запросами)

Нужно:

* троттлинг: 0.5–1.0 сек
* очередная обработка

---

## 4.5. API Endpoints

### `POST /api/upload`

Принимает ZIP
Возвращает:

```json
{ "session_id": "abc123" }
```

### `GET /api/auth/telegram-login`

Проверка Telegram Login подписи.

### `POST /api/parse`

Запускает парсер WhatsApp.

### `GET /api/contacts`

Возвращает список контактов Telegram.

### `POST /api/migrate`

Запускает миграцию.
Параметры:

* `session_id`
* `target_chat_id`

### `GET /api/migrate/status`

Возвращает:

```json
{
  "total": 1234,
  "processed": 542,
  "percent": 43.9,
  "current_action": "sending video 34.mp4",
  "errors": []
}
```

---

# 5. Функциональность frontend (Vue.js)

## 5.1. Структура страниц

1. `/` — Welcome
2. `/upload` — загрузка ZIP
3. `/auth` — Telegram login
4. `/select-chat` — выбор чата
5. `/progress` — статус миграции
6. `/done` — результат

## 5.2. Компоненты

* FileUpload
* TelegramLoginButton
* ChatList
* ProgressBar
* StatusLog
* ErrorDialog

## 5.3. Состояния

Сохраняем:

```js
{
  sessionId: "",
  userTelegramId: "",
  parsedMessagesCount: 0,
  selectedChatId: "",
}
```

---

# 6. Нефункциональные требования

## 6.1. Безопасность

* Сессии Telegram нельзя хранить в базе, только в файловой системе с ограниченным доступом.
* Все временные файлы удаляются после окончания миграции.
* HTTPS обязателен.

## 6.2. Ограничения

* Максимальный размер ZIP: 20GB
* Максимальный размер одного медиафайла: 2GB (лимит Telegram)
* Скорость зависит от скорости отправки MTProto, ориентир: 10–40 сообщений/мин

## 6.3. Отказоустойчивость

* Если миграция прерывается, можно **продолжить**
* Очередь сообщений хранится в `session.json`

---

# 7. Структура проекта

## Backend

```
backend/
  app/
    main.py
    api/
      upload.py
      parse.py
      auth.py
      telegram.py
      migrate.py
    core/
      config.py
      security.py
    services/
      whatsapp_parser.py
      telegram_client.py
      file_manager.py
      migration_manager.py
  sessions/
  tmp/
  requirements.txt
```

## Frontend

```
frontend/
  src/
    pages/
    components/
    store/
    api/
  package.json
```
